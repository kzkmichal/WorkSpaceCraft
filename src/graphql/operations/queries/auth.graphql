query Me {
	me {
		...UserFields
	}
}

query GetSession {
	session {
		...SessionFields
	}
}

# query AllUsers($limit: Int, $offset: Int) {
# 	allUsers(limit: $limit, offset: $offset) {
# 		...UserFields
# 	}
# }
