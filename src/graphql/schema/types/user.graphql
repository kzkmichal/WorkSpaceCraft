type User {
	id: ID!
	name: String
	email: String!
	image: String
	emailVerified: DateTime
	products: [Product!]
	reviews: [Review!]
	# setups: [Setup!]
	# articles: [Article!]
	createdAt: DateTime!
	updatedAt: DateTime!
}
