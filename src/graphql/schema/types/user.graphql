type User {
	id: ID!
	name: String!
	email: String!
	# password: String!
	# products: [Product!]!
	products: [Product]
	# reviews: [Review!]!
	createdAt: String!
	updatedAt: String!
	# setups: [Setup!]!
}
