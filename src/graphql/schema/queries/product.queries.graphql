type Query {
	products(
		limit: Int
		offset: Int
		categoryType: CategoryType
		subcategoryFullSlug: String
		tagSlugs: [String!]
	): [Product!]!
	product(id: ID!): Product
	myProducts: [Product!]!
}
